#!/usr/bin/pyhton3
def search_replace(my_list, search, replace):
    new_list = my_list[:]
    for a in range(len_list)):
        if new_list[a] == search:
            new_list[a] = replace
    return (new_list)
